language: java
jdk:
  - openjdk8
branches:
  only:
    - master

# Travis CI 서버의 HOME
cache:
  directories:
    -'$HOME/.m2/repository'
    -'$HOME/.gradle'

# master 브랜치가 푸시되면 수행되는 명령어
script: "./gradlew clean build -x test"

# CI 실행완료시 메일로 알람
notifications:
  email:
    recipients:
      - dldydtjs8124@gmail.com